function JSONParser(body) {
    this.data = JSON.parse(data);
}

JSONParser.prototype.getProperty = function(name){
};

JSONParser.prototype.getChildProperty = function(name, parent){
}

module.exports = JSONParser;